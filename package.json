{
  "dependencies": {
    "jest": "^29.7.0"
  },
  "scripts": {
    "test": "jest",
    "build": "CI=false && mkdir -p dist && cp index.html forca.js public/"
  },
  "devDependencies": {
    "esbuild": "^0.25.6"
  }
}
