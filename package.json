{
  "dependencies": {
    "jest": "^29.7.0"
  },
  "scripts": {
    "test": "jest",
    "build": "mkdir -p dist && cp index.html forca.js dist/"
  },
  "devDependencies": {
    "esbuild": "^0.25.6"
  }
}
